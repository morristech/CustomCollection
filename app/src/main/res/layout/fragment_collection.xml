<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent">
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal">
        <EditText
            android:id="@+id/main_collection_name"
            android:layout_width="match_parent"
            android:layout_height="48dp"
            android:padding="8dp"
            android:layout_weight=".2"
            android:hint="@string/new_collection_name"
            android:textColor="@color/white"
            android:textColorHint="@color/transparent_white_percent_25"
            android:background="@color/transparent_black_percent_50"
            tools:text="Hello World!"
            />
        <ImageButton
            android:id="@+id/main_collection_add_button"
            android:layout_width="match_parent"
            android:layout_height="48dp"
            android:scaleType="fitCenter"
            android:layout_weight=".8"
            android:src="@drawable/greenadd"
            android:background="@color/transparent_black_percent_50"
            />
    </LinearLayout>
    <ListView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="12dp"
        android:id="@+id/collection_listview"
        android:background="@color/transparent_black_percent_50"
        android:divider="@android:color/transparent"
        android:dividerHeight="3dp"
        ></ListView>
</LinearLayout>